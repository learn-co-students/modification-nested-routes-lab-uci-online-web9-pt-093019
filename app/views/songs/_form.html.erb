<%= form_for @song  do |f| %>
  
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  
  <% if @artists %>
    <%= artist_select(@song, @artists) %>
  <% else %>
    <%= label_tag @song.artist.name %>
  <% end %>
  <br />

  <%= f.submit %>
<% end %>
